# Prog08: Create a program that ask the user to input their fullname. Print the number of characters in the input.
# Get user input full name, print number of characters. (including space, use len())

user_input = input("Enter your fullname: ")

print(len(user_input))